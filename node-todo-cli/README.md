`npm run build`

`npm link`

```
➜  node-todo-cli git:(main) ✗ notes --help
notes <command>

Commands:
  notes new <note>     create a new note
  notes all            get all notes
  notes find <filter>  get matching notes
  notes remove <id>    remove a note by id
  notes web [port]     launch website to see notes
  notes clean          remove all notes

Options:
      --help     Show help                           [boolean]
      --version  Show version number                 [boolean]
  -t, --tags     tags to add to the note              [string]
```